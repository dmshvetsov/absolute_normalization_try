<%= form_for(surname) do |f| %>
  <% if surname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(surname.errors.count, "error") %> prohibited this surname from being saved:</h2>

      <ul>
      <% surname.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %>
    <%= f.text_field :data %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
